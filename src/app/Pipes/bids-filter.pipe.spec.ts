import { BidsFilterPipe } from './bids-filter.pipe';

describe('BidsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BidsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
